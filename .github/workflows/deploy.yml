
name: Deployment
concurrency: live

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-24.04-arm
    permissions:
      id-token: write
      contents: read
    environment: live
    steps:
    - name: Checkout
      uses: actions/checkout@v7
    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: '11.x'
    - name: Build
      run: dotnet workload restore && dotnet workload install wasm-tools && dotnet publish web-asn.csproj -c release --output bin/publish
    - name: Tailscale
      uses: tailscale/github-action@v4
      with:
        oauth-client-id: ${{ secrets.TS_OIDC_CLIENT_ID }}
        audience: ${{ secrets.TS_OIDC_AUDIENCE }}
        tags: tag:ci
    - name: Copy To Production
      run: |
        mkdir -p ~/.ssh
        echo "vcsjones-dev ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKK5Bi4tOamAsNJpQpmk3bBwDN0hEB8dHTaeOdtJN6OU" >> ~/.ssh/known_hosts
        rsync -a --delete bin/publish/wwwroot/ deployer@vcsjones-dev:/var/wwwroot/asn.vcsjones.dev
        rsync --ignore-times _server/asn.vcsjones.dev.Caddyfile deployer@vcsjones-dev:/etc/caddy/caddy.d/asn.vcsjones.dev.Caddyfile
        ssh deployer@vcsjones-dev 'sudo systemctl reload caddy.service'

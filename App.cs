using Microsoft.AspNetCore.Components;

namespace WebAsn;

// We need a partial class here because the C# VS Code extension doesn't appear to see the generated one.
// So put this here to make red squigglies go away.
partial class App : ComponentBase {
}
